app:
  security:
    jwt:
      secret: ${JWT_SECRET}

logging:
  level:
    root: INFO

server:
  port: ${SERVER_PORT:8080}

spring:
  application:
    name: tech-events-backend
  jpa:
    open-in-view: false
  profiles:
    default: dev