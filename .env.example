# copy this file to .env and update the values

SERVER_PORT=8080
SPRING_PROFILES_ACTIVE=dev

POSTGRES_DB=techevents_dev
DB_URL=jdbc:postgresql://localhost:5432/techevents_dev
DB_USERNAME=devuser
DB_PASSWORD=devpass

JWT_SECRET=replace-with-secure-secret

# Docker variables
POSTGRES_PASSWORD=devpass
POSTGRES_USER=devuser
POSTGRES_DB=techevents_dev

# Production actuator variables
ACTUATOR_USERNAME=actuator
ACTUATOR_PASSWORD=actuator

# Optional production variables
# CORS_ALLOWED_ORIGINS=https://dashboard.example.com
# STORAGE_BASE_PATH=/var/app/storage

# pgAdmin
# PGADMIN_EMAIL=pgadmin@example.com
# PGADMIN_PASSWORD=pgadmin
# PGADMIN_PORT=5050